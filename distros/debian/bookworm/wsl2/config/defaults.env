# distros/debian/bookworm/wsl2/config/defaults.env

# -----------------------
# Python (via pyenv)
# Note: Python has no official "LTS". Using a major.minor prefix makes pyenv pick
# the latest available patch (e.g., 3.13 -> 3.13.x newest).
# -----------------------
PYTHON_DEFAULT="3.13"
PYTHON_ALT="3.14"

# -----------------------
# Java (via SDKMAN)
# Auto-pick the latest LTS for the selected vendor (Temurin by default).
# -----------------------
JAVA_LTS_MODE="auto"        # auto | fixed
JAVA_VENDOR_REGEX="tem"     # Temurin identifier contains "-tem"
JAVA_LTS_FALLBACK="21"      # fallback if auto-detection fails

# If you switch to fixed mode, set the major you want (e.g., 25 or 21)
JAVA_LTS_MAJOR_DEFAULT="25"

# -----------------------
# Gradle (via SDKMAN)
# No true LTS; use latest stable by default.
# -----------------------
GRADLE_MODE="latest"        # latest | fixed
GRADLE_VERSION="9.3.1"      # used only when GRADLE_MODE="fixed"

# -----------------------
# Node.js (optional, via nvm)
# "lts/*" always installs the latest LTS line.
# -----------------------
NODE_LTS_ALIAS="lts/*"
NVM_VERSION="v0.40.4"